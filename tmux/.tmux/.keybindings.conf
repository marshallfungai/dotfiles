## ~/.dofiles/tmux/.keybindings.conf - Keybindings

# -----------------------------
#  Keybindings
# -----------------------------

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Easier Detach
bind d detach-client
